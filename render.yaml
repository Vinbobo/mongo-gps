services:
  - type: web
    name: gps-checkin-mongo
    env: python
    buildCommand: ""
    startCommand: gunicorn app:app
    plan: free
    envVars:
      - key: MONGO_URI
        sync: false
      - key: DB_NAME
        sync: false
